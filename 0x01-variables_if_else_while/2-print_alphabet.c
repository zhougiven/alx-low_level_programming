#include <stdio.h>
#include <stdlib.h>
/**
 * main -prints all letter of the alphabet in lower cases.
 * Return: Always 0.
 */
int main(void)
{
	char ch;

	for (ch = 'a'; ch <= 'z'; ch++)
	putchar (ch);
	return (0);
}
