#include <stdio.h>

/**
 * main - prints all single digit numbers
 * of base 10.
 * variable char cannot be used.
 * only can use putchar.
 * Return: Always 0.
 */

int main(void)
{
	int n;

	for (n = 48; n <= 57 n++)
	putchar (n);
	putchar ('\n');
	return (0);
}
