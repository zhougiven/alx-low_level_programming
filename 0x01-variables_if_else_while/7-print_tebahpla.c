#include <stdio.h>
#include <stdlib.h>

/**
 * main - prints the lower case alphabets
 * in reverse.
 * Only putchar can be used.
 * Return: Always 0.
 */

int main(void)
{
	char ch = 'z';

	while (ch >= 'a')
	putchar (ch);
	putchar ('\n);
	return (0);
}
