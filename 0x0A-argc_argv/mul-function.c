#include "main.h"
#include <stdio.h>
#include <stdlib.h>

/**
 * mul - multiplies two numbers.
 * @i: operand.
 * @j: operand.
 * @res: result of the multiplication of both operations.
 *
 * Return: Res
 */

int mul(int i, int j)
{
	int res;

	res = i * j;
	return (res);
}
