#include "main.h"
#include <stdio.h>

/**
 * mul - multiplies two integers.
 * @a: input variable for the mul expreesion.
 * @b: input variable for the mul expression.
 * Return: success (0),
 */

int mul(int a, int b)
{
	int result;

	result = (a * b);
	return (result);
}
